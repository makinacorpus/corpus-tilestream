{% set data = salt['mc_utils.json_load'](data)%}
{%- set data = data.extra.data -%}
location / {
    proxy_pass http://tiles;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header Host $http_host;
    proxy_redirect off;
}
